**What changed and why**

**How it was tested** (which suite, which new test)

**On-disk format changed?** no / yes — `FORMAT` bumped and CHANGELOG updated

**Benchmark before/after** (only for `engine.rs` / `storage.rs` changes)

- [ ] `cargo fmt` and `cargo clippy --all-targets -- -D warnings` are clean
- [ ] No new dependencies, no `unsafe`
